services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: maxcloud
      POSTGRES_PASSWORD: maxcloud
      POSTGRES_DB: maxcloud
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
